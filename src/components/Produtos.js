import React from "react"
import Menu from "./Menu"

const Produtos = () => {
    return(
        <div>
            <h2>Produtos</h2>
            <Menu />
        </div>
    )
}

export default Produtos